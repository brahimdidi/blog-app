<h1>Users#users</h1>
<ul class= 'list-group'>

<% @users.each do |user| %>
  <li class= 'list-group-item d-flex justify-content-around align-items-center'>
   name : <%= user.name %>
   <span>number of posts : <%= user.posts.length %> </span> 
   <%= link_to "see more", user%>
   </li>
   <div>
   </div>
  <% end %>
</ul>

