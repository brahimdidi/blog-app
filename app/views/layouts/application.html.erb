<!DOCTYPE html>
<html>
  <head>
    <title>BlogApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <link rel="stylesheet" 
    href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Anek+Telugu:wght@200;300&family=Roboto:wght@300&display=swap" rel="stylesheet">
     <link rel='stylesheet' href= 'https://cdn.jsdelivr.net/npm/bootstrap@4.4.1/dist/css/bootstrap.min.css'>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class='bg-grey'>
    <div class= 'container bg-light mb-2'>
      <%= render 'layouts/navbar' %>
      <% if notice %>
      <p class= 'alert alert-secondary'><%= notice%> </p>
      <script>
      // this function changes the alert color incase of error
        (function() {
          let alertElement = document.querySelector('.alert')
          let text = alertElement.textContent;
          text = text.toLowerCase()
          console.log(text)
          if (text.includes('error')) {
            alertElement.classList.replace('alert-secondary','alert-danger')
          };
        })();
     // this function auto remove the alert after 2 seconds
        setTimeout (function () {
        let alertElement = document.querySelector('.alert')
        alertElement.remove()
        }, 2000);
      </script>
      <%end%>
      <%= yield %>
    </div>
  </body>
</html>
